# Quickstart

No setup changes required for this feature.
1. Run `npx expo start` to test the modal UI interactively.
2. Enter the create routine flow from the workouts screen to see the updated Sets/Reps modal.
