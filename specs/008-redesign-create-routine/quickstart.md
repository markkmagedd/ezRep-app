# Quickstart

- Step 1: Execute `/speckit.tasks` to generate technical tasks.
- Step 2: Implement the fixes within `CreateRoutineScreen.tsx`, including layout cleanup and the custom expandable bottom sheet logic.
- Step 3: Run the app via Expo using `npx expo start --clear` and manually test by adding 15+ exercises to trigger overflow behaviors. Make sure the sticky bottom button is accessible.
