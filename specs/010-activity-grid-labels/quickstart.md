# Quickstart: Activity Grid Labels

This feature modifies the horizontal scrollable grid on the Home screen.

There is no new setup or installation required. Simply start the existing expo project:

```bash
npx expo start
```

And navigate to the Home screen showing the `YearlyConsistencyGrid` to verify all 7 day labels and top-edge month abbreviations render correctly without vertical layout breaking.
